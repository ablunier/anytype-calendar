{
  "extends": "../../tsconfig.package.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    // No ambient type packages, as in every context: `fetch` is injected by the
    // composition root rather than taken from the platform.
    "types": []
  },
  "include": ["infrastructure/**/*"],
  // Tests import vitest, which would otherwise be emitted into dist/. Vitest transpiles
  // them directly from source.
  "exclude": ["**/*.test.ts", "dist"]
}
