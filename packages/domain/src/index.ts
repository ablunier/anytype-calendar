// Public surface of the domain layer.
//
// This package has zero dependencies by construction — no Electron, no React, no HTTP
// client, no npm runtime deps, no Node core modules. `npm run lint:arch` enforces that.
//
// Re-export from ./event, ./mapping, ./layout and ./ports as those are filled in.

export {}
