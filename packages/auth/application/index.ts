// Public surface of the auth context's application layer: @anytype-calendar/auth/application.
//
// Use cases, orchestrating the domain through the ports it declares. Depends on this
// context's domain (../domain) and nothing else; adapters are injected by the composition
// root, never imported.

export {}
