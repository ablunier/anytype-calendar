// Public surface of the auth context's infrastructure layer: @anytype-calendar/auth/infrastructure.
//
// Driven adapters: concrete implementations of this context's domain ports. Depends on
// this context's domain (../domain) and nothing else. Grouped by technology — ./in-memory
// for simulated adapters, ./anytype for the Anytype local API once it lands.

export {}
